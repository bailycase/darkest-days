#pragma once
// EntityManager.Register(entity)

const int MAX_ENTITIES = 5000;

class EntityManager
{
public:
    // EntityManager();
    // int CreateEntity();
    // void DestroyEntity();

private:
    // a queue of unused entity ID's
    // std::queue<Entity> m_AvailableEntities{};

    // Array of signatures where the index correspons to the entity ID
    // std::array<Signature, MAX_ENTITIES> m_Signatures{};

    // Total "living" entities - used to keep limits on how many entities exist
    // int m_LivingEntityCount{};
};